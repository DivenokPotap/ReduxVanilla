export const getCurrentFilter = (state) => state.filter.filter;
export const getCurrentCounter = (state) => state.count.count;
export const getCurrentBalance = (state) => state.account.balance;
export const getCurrentLang = (state) => state.locale.lang;